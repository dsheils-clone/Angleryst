apiVersion: apps/v1
kind: Deployment
metadata:
  name: catch-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: catch-service
  template:
    metadata:
      labels:
        app: catch-service
    spec:
      containers:
        - name: catch-service
          image: angleryst/catch-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8091
          envFrom:
            - configMapRef:
                name: angleryst-config
          env:
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: angleryst-secret
                  key: db-password
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: angleryst-secret
                  key: jwt-secret
          readinessProbe:
            tcpSocket:
              port: 8091
            initialDelaySeconds: 20
            periodSeconds: 10
            failureThreshold: 5
---
apiVersion: v1
kind: Service
metadata:
  name: catch-service
spec:
  selector:
    app: catch-service
  ports:
    - port: 8091
      targetPort: 8091
