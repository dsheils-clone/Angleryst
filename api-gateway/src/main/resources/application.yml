spring:
  application:
    name: api-gateway

server:
  port: 8080

gateway:
  routes:
    users: ${USER_SERVICE_URL:http://localhost:8090}
    catches: ${CATCH_SERVICE_URL:http://localhost:8091}
    tackle: ${TACKLE_SERVICE_URL:http://localhost:8092}
    inventory: ${INVENTORY_SERVICE_URL:http://localhost:8093}
    recommendations: ${RECOMMENDATION_SERVICE_URL:http://localhost:8094}
